/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
            {hostname: "marvelous-starfish-987.convex.cloud"}
        ]
    }
};

export default nextConfig;
